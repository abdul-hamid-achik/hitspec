version: '3'

vars:
  BINARY_NAME: hitspec
  VERSION:
    sh: git describe --tags --always --dirty 2>/dev/null || echo "dev"
  BUILD_TIME:
    sh: date -u '+%Y-%m-%d_%H:%M:%S'
  LDFLAGS: -ldflags "-X main.version={{.VERSION}} -X main.buildTime={{.BUILD_TIME}}"
  MAIN_PATH: ./apps/cli

tasks:
  default:
    desc: Show available tasks
    cmds:
      - task --list

  # ---------------------------------------------------------------------------
  # Top-level unified lifecycle tasks
  # ---------------------------------------------------------------------------

  setup:
    desc: Install all Go dependencies and generate code
    cmds:
      - task: go:deps
      - task: generate

  deps:
    desc: Download and tidy Go dependencies
    cmds:
      - task: go:deps

  generate:
    desc: Run go generate
    cmds:
      - go generate ./...

  build:
    desc: Build Go CLI binary
    cmds:
      - task: go:build

  test:
    desc: Run all Go tests
    cmds:
      - task: go:test

  e2e:
    desc: Run studio end-to-end tests (PTY behavior specs via glyphrun's `glyph`)
    cmds:
      - go build -o ./e2e/bin/hitspec ./apps/cli
      - glyph run e2e/specs/glyphrun/*.yml --format md
    preconditions:
      - sh: command -v glyph
        msg: "glyph (glyphrun) is not installed — see https://github.com/abdul-hamid-achik/glyphrun"

  lint:
    desc: Run Go format, vet, and golangci-lint
    cmds:
      - task: go:lint

  security:
    desc: Run Go security checks
    cmds:
      - task: go:security

  check:
    desc: Run all checks (lint + test + security)
    cmds:
      - task: lint
      - task: test
      - task: security

  clean:
    desc: Clean all build artifacts
    cmds:
      - rm -rf bin/
      - rm -f coverage.out coverage.html

  # ---------------------------------------------------------------------------
  # Go tasks
  # ---------------------------------------------------------------------------

  go:deps:
    desc: Download and tidy Go modules
    cmds:
      - go mod download
      - go mod tidy

  go:deps:update:
    desc: Update all Go dependencies
    cmds:
      - go get -u ./...
      - go mod tidy

  go:build:
    desc: Build the Go CLI binary
    deps: [generate]
    cmds:
      - go build {{.LDFLAGS}} -o bin/{{.BINARY_NAME}} {{.MAIN_PATH}}
    sources:
      - '**/*.go'
    generates:
      - bin/{{.BINARY_NAME}}

  go:test:
    desc: Run Go tests
    cmds:
      - go test -v -race -cover ./...

  go:test:coverage:
    desc: Run Go tests with coverage report
    cmds:
      - go test -v -race -coverprofile=coverage.out ./...
      - go tool cover -html=coverage.out -o coverage.html
      - echo "Coverage report generated at coverage.html"

  go:test:unit:
    desc: Run Go unit tests only (short mode)
    cmds:
      - go test -v -race -short ./...

  go:test:integration:
    desc: Run Go integration tests
    cmds:
      - go test -v -race -run Integration ./...

  go:lint:
    desc: Run Go linters (fmt check + vet + golangci-lint)
    cmds:
      - test -z "$(gofmt -l .)" || { echo "Files need formatting:"; gofmt -l .; exit 1; }
      - go vet ./...
      - golangci-lint run ./...

  go:security:
    desc: Run Go security scanners
    cmds:
      - gosec ./...
      - govulncheck ./...

  # ---------------------------------------------------------------------------
  # Development tasks
  # ---------------------------------------------------------------------------

  install:
    desc: Install the CLI to GOPATH/bin
    cmds:
      - go install {{.LDFLAGS}} {{.MAIN_PATH}}

  run:
    desc: Run the CLI (use -- to pass args, e.g., task run -- run examples/simple.http)
    cmds:
      - go run {{.MAIN_PATH}} {{.CLI_ARGS}}

  dev:
    desc: Run in development mode with example file
    cmds:
      - go run {{.MAIN_PATH}} run examples/petstore/petstore.http -v

  watch:
    desc: Watch for changes and re-run tests
    cmds:
      - watchexec -e go -r -- go test -v ./...

  watch:run:
    desc: Watch for changes and re-run CLI
    cmds:
      - watchexec -e go,http -r -- go run {{.MAIN_PATH}} run examples/petstore/petstore.http -v

  studio:dev:
    desc: Run the interactive terminal app
    cmds:
      - go run {{.MAIN_PATH}} studio {{.CLI_ARGS}}

  serve:dev:
    desc: Run the REST/WebSocket API server
    cmds:
      - go run {{.MAIN_PATH}} serve --api-only {{.CLI_ARGS}}

  docs:dev:
    desc: Run Mintlify docs locally
    dir: apps/docs
    cmds:
      - npx mintlify dev

  # ---------------------------------------------------------------------------
  # Benchmarks
  # ---------------------------------------------------------------------------

  bench:
    desc: Run all benchmarks
    cmds:
      - go test -bench=. -benchmem ./...

  bench:parser:
    desc: Run parser benchmarks
    cmds:
      - go test -bench=. -benchmem ./packages/core/parser/...

  # ---------------------------------------------------------------------------
  # Release tasks
  # ---------------------------------------------------------------------------

  build:all:
    desc: Build for all platforms
    deps: [generate]
    cmds:
      - GOOS=linux GOARCH=amd64 go build {{.LDFLAGS}} -o bin/{{.BINARY_NAME}}-linux-amd64 {{.MAIN_PATH}}
      - GOOS=linux GOARCH=arm64 go build {{.LDFLAGS}} -o bin/{{.BINARY_NAME}}-linux-arm64 {{.MAIN_PATH}}
      - GOOS=darwin GOARCH=amd64 go build {{.LDFLAGS}} -o bin/{{.BINARY_NAME}}-darwin-amd64 {{.MAIN_PATH}}
      - GOOS=darwin GOARCH=arm64 go build {{.LDFLAGS}} -o bin/{{.BINARY_NAME}}-darwin-arm64 {{.MAIN_PATH}}
      - GOOS=windows GOARCH=amd64 go build {{.LDFLAGS}} -o bin/{{.BINARY_NAME}}-windows-amd64.exe {{.MAIN_PATH}}

  release:snapshot:
    desc: Create a snapshot release (no publishing)
    cmds:
      - goreleaser release --snapshot --clean

  release:
    desc: Create and publish a release
    cmds:
      - goreleaser release --clean
